<!--pages/sort/sort.wxml-->

<view class='container'>
  <!--导航条-->
  <!-- <view class="navbar">
    <text wx:for="{{navbar}}" data-idx="{{index}}" id="{{item.id}}" class="item {{currentTab==index ? 'active' : ''}}" wx:key="unique" bindtap="navbarTap">{{item.name}}</text>
  </view> -->

  <view class='goods_list'>
    <block wx:for="{{goodsList}}" wx:key="{{index}}">  <!-- wx:if="{{currentTab==0}}" -->
      <view class='goods_item' data-id="{{item.goodsId}}" bindtap="toGoodsDetails">
        <view class='goods_img'>
          <image src="{{item.goodsImg}}"></image>
        </view>
        <view class='goods_info'>
          <view class='goods_name'>{{item.goodsName}}</view>
          <view class='goods_price_sold'>
            <text class='goods_price'>￥{{item.goodsPrice}}</text>
            <text class='goods_sold'>销量：{{item.goodsSold}}</text>
          </view>
        </view>
      </view>
    </block>
  </view>

</view>